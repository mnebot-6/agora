package com.app.community.core.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

/**
 * Formas inspiradas en el orden jonico griego.
 * Curvas suaves que evocan marmol pario pulido y volutas.
 * Mas redondeado que el dorico — la elegancia jonica
 * se refleja en esquinas graciles.
 */
val AgoraShapes = Shapes(
    extraSmall = RoundedCornerShape(6.dp),
    small = RoundedCornerShape(8.dp),
    medium = RoundedCornerShape(12.dp),
    large = RoundedCornerShape(16.dp),
    extraLarge = RoundedCornerShape(20.dp),
)
